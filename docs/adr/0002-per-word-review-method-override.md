# 0002 — 단어별 복습 방식 오버라이드

전역 설정된 복습 방식(레이니어/고정간격/SM-2)을 개별 단어 수준에서 오버라이드할 수 있게 결정했습니다. 사용자가 "이 단어는 매일 보고 싶다"처럼 특정 단어의 복습 방식을 변경할 수 있도록 하기 위함입니다.

ReviewCard 모델에 `overrideMethod` nullable 필드를 추가하여, null이면 전역 설정을 따르고, 값이 있으면 해당 값을 사용합니다. 기존 `reviewMethod` 필드는 전역 설정 용도로 유지됩니다.
