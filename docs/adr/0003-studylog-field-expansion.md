# 0003 — 학습 이력 필드 확장

학습 이력(StudyLog) 테이블에 세 가지 필드를 추가하기로 결정했습니다: `studyMethod`(학습 방식), `durationMs`(소요 시간), `answerType`(정답 유형).

학습 방식은 플래시카드/퀴즈/뜻타이핑/철자타이핑을 구분하여, 어떤 학습 도구가 가장 효과적인지 분석할 수 있게 합니다. 소요 시간은 학습 패턴 분석에, 정답 유형(완전 정답/부분 정답/오답)은 정밀한 학습 진행도 측정에 사용됩니다.

DB 버전을 올려 마이그레이션에서 필드를 추가합니다.
